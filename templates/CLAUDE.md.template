# {{PROJECT_NAME}} — Router

Project instructions for `{{PROJECT_NAME}}`.

**KISS. YAGNI.** Direct answers. Simplest thing that works. Don't build what isn't asked for.

## Hard Rules (always on)

1. **Two Strikes** — same op fails twice → STOP. Research, log the failure mode, report. No blind third retry. Trigger `five-whys` instead.
2. **Blocker Protocol** — hit a blocker, stop and report. Explain what was tried. Ask before working around.
3. **KISS / YAGNI** — Reuse before you write. Pick the simplest mechanism. Don't build for hypothetical futures.
4. **Read before writing** — understand existing context (file, schema, prior version) before producing output.
5. **No fabrication** — never invent quotes, citations, file paths, IDs, or APIs. If unsure, say so.
6. **Quality gate before irreversible decisions** — schema changes, MCP/agent architecture, public-facing copy, anything that compounds → run `quality-review`.
7. **Audit your writes** — after every write, restate what the tool returned (IDs, paths, params). No self-grading; cite the actual response.

## The Router

Every non-trivial request: **Classify → Load → Think → Pre-flight Checklist → Validate.** Full ritual: `.claude/rules/routing-protocol.md`.

| If request touches… | Load |
|---|---|
| A plan, design, PRD, architecture decision, or other proposal before commit | `quality-review` skill (parallel adversarial team) |
| Just KISS/YAGNI lens on existing code or design | `simplify` command |
| Just "challenge my reasoning" lens | `adversarial` command |
| Edge cases / failure modes / "what breaks this" | `chaos` command |
| Something failed unexpectedly, retry-blind temptation | `five-whys` skill |
| Client-facing prose, deliverables, emails ≥3 sentences | `writing-quality` skill |
| Validating a skill against a target repo | `skill-validator` skill (ralph loop) |
| Writing a PRD with binary success criteria | `prd-writer` skill (Stage 1) |
| Decomposing an accepted PRD into a task plan | `task-plan` skill (Stage 2) |
| Validating the plan before implementation | `quality-review` skill (Stage 3) |
| Running the implementation loop against a task plan | `ralph-implement` skill (Stage 4) |
| Running the verification gate against PRD criteria | `verify` skill (Stage 5) |
| Evaluating the built implementation | `quality-review` skill (Stage 6) |
| A valuable insight or decision from this session worth keeping | `insight-crystallizer` skill |
| An insight that should be always-on governance, not just retrievable | `insight-promotion` skill |

Multi-domain → load multiple. Domain-specific governance (Notion, Gmail, Drive, Supabase, etc.) belongs in a project-specific rule file or Cowork plugin, NOT in this template.

## Agents

**Lane 1 — Quality**

| Agent | Model | Role |
|---|---|---|
| `adversarial-reviewer` | Opus | Challenges claims and assumptions |
| `simplifier` | Sonnet | KISS / YAGNI lens |
| `chaos-engineer` | Sonnet | Robustness / edge-case lens |

The three lenses are non-overlapping. `quality-review` runs them in parallel.

**Lane 2 — Code**

| Agent | Model | Role |
|---|---|---|
| `code-reviewer` | Sonnet | Quality review of a diff or file set |
| `debugger` | Sonnet | Reproduction-first bug diagnosis |
| `code-analyzer` | Sonnet | Cross-file logic-flow analysis |
| `security-auditor` | Opus | OWASP-style threat audit |

**Lane 3 — Orchestration**

| Agent | Model | Role |
|---|---|---|
| `workflow-orchestrator` | Sonnet | Sequences multi-stage workflows |
| `multi-agent-coordinator` | Sonnet | Tracks parallel agent state; handles partial failures |
| `agent-organizer` | Sonnet | Picks which agents fit a task |
| `task-distributor` | Sonnet | Splits work across parallel agents safely |
| `context-manager` | Sonnet | Manages shared context across handoffs |
| `error-coordinator` | Sonnet | Correlates failures to find shared root causes |
| `knowledge-synthesizer` | Sonnet | Combines multi-agent outputs into a single report |
| `performance-monitor` | Sonnet | Surfaces hotspots in agent runtime |

**Lane 4 — Research**

| Agent | Model | Role |
|---|---|---|
| `research-analyst` | Sonnet | Multi-source synthesis with cited claims |
| `search-specialist` | Haiku | Quick precision lookups |
| `evidence-auditor` | Sonnet | Verifies quotes and citations |
| `metadata-fetcher` | Haiku | Mechanical metadata lookups |

## Key Paths

| Path | What |
|---|---|
| `.claude/rules/routing-protocol.md` | Full 5-step ritual |
| `.claude/rules/kiss-yagni.md` | Principles, Two Strikes, Blocker Protocol |
| `.claude/agents/quality/` | Review team — adversarial-reviewer, simplifier, chaos-engineer |
| `.claude/agents/code/` | Code agents — code-reviewer, debugger, code-analyzer, security-auditor |
| `.claude/agents/orchestration/` | 8 orchestration agents for multi-agent and multi-stage workflows |
| `.claude/agents/research/` | research-analyst, search-specialist, evidence-auditor, metadata-fetcher |
| `.claude/skills/` | quality-review, five-whys, writing-quality, skill-validator, prd-writer, task-plan, ralph-implement, verify, insight-crystallizer, insight-promotion |
| `.claude/commands/` | /quality-review, /simplify, /adversarial, /chaos, /five-whys |
| `.claude/hooks/` | compact-recovery.sh, kiss-yagni-reminder.py |
| `docs/PRDs/` | Project PRDs and plans (templates at `docs/PRDs/_templates/`) |

## Project-Specific Setup

When initializing this template for a new project, fill in:

- **Project name** above (`{{PROJECT_NAME}}`).
- **Domain rules** in `.claude/rules/` for any tools the project uses (Supabase, Notion, etc.). Keep them small — auto-load is for always-relevant content only.
- **ROADMAP.md** at repo root if you want the compact-recovery hook to re-inject it (default: enabled).
- **Custom skills** in `.claude/skills/`. Don't duplicate what's already here.
